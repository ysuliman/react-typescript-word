export interface TileRowProps {
    wordLength: number,
    rowIndex: number,
    currentGuess: string,
    activeGuessIndex: number,
    isShakeRow: boolean,
    isDanceRow: boolean,
    isFlipRow: boolean
}
